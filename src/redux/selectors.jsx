export const selectCampers = state => state.campers.items;

export const selectPage = state => state.campers.page;

export const selectIsLoading = state => state.campers.isLoading;

export const selectBackgroundImage = state => state.campers.backgroundImage;

export const selectFavorItems = state => state.campers.favorItems;

export const selectShowLoadMoreButCatalog = state => state.campers.showLoadMoreButCatalog;

export const selectScrollValueCatalog = state => state.campers.scrollValueCatalog;

export const selectValueAC = state => state.campers.filters.valueAC;

export const selectValueTransm = state => state.campers.filters.valueTransm;

export const selectValueKitch = state => state.campers.filters.valueKitch;

export const selectValueTV = state => state.campers.filters.valueTV;

export const selectValueShower = state => state.campers.filters.valueShower;

export const selectValueLocation = state => state.campers.filters.location;

export const selectValueVan = state => state.campers.filters.valueVan;

export const selectValueFullIntedr = state => state.campers.filters.valueFullIntedr;

export const selectValueAlcove = state => state.campers.filters.valueAlcove;
